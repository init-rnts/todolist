<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ToDo List</title>
</head>
<body>

    <% if (day === "Saturday" || day === "Sunday") { %>
        <h1 style="color: green">Today is <%= day %>.</h1>
    <% } else { %> 
        <h1 style="color: red">Today is <%= day %>.</h1>
    <% } %>

</body>
</html>